#include <stdio.h>

int main(){

    // arithmetic oprator

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ incremet
    // -- decrement

    int x = 5;
    int y = 2;

    //int z = x + y;
    //int z = x - y;
    //int z = x * y;
    //int z = x / (float) y;
    //int z = x % y;

    x++;
    y--;

    printf("%d\n", x);

    return 0;
}